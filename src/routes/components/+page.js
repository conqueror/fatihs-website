export const csr = true; // Enable client-side rendering for this page

// This ensures that the components with animations and interactivity
// will work properly by rendering them on the client instead of 
// during server-side rendering

export function load() {
  return {};
} 